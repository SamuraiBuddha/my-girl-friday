# Azure App Registration Credentials
# Get these from https://portal.azure.com → App registrations
OUTLOOK_CLIENT_ID=your-application-client-id-here
OUTLOOK_CLIENT_SECRET=your-client-secret-value-here
OUTLOOK_TENANT_ID=your-directory-tenant-id-here

# Optional: Redirect URI (defaults to http://localhost:8080)
# OUTLOOK_REDIRECT_URI=http://localhost:8080

# Optional: Authentication method
# For personal accounts: consumers
# For work/school accounts: organizations  
# For both: common (default)
# OUTLOOK_AUTHORITY=common

# Optional: Token cache location
# TOKEN_CACHE_FILE=token_cache.json

# Optional: Debug mode
# DEBUG=false
